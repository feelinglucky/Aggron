<div class="blog" id="blog">
    <h2><i class="icon-pencil"></i>博客</h2>
    <ul>
        {{# posts_latest }}
        <li>
            <a href="{{url}}">{{title}}</a>
        </li>
        {{/ posts_latest }}
    </ul>

    <ul>
        {{# categories }}
        <li>
            <a href="{{ url }}">{{ name }} <span>({{ count }})</span></a>
        </li>
        {{/ categories }}
    </ul>

    <ul>
        {{# tags }}
        <li>
            <a href="{{ url }}">{{ name }} <span>{{ count }}</span></a>
        </li>
        {{/ tags }}
    </ul>
</div>
